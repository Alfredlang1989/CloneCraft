# CloneCraft

Data-driven C++ voxel engine prototype using OgreNext.

The repository is initialized for the current CloneCraft source tree. See `INDEX.plan` and `docs/` for architecture and project status.
